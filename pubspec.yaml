name: neom_booking
description: Booking and Directory for Cyberneom.
version: 1.2.0
homepage: https://github.com/emmanuel-montoya/neom_booking
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  neom_commons:
    git:
      url: git@github.com:Cyberneom/neom_commons.git
  neom_posts:
    git:
      url: git@github.com:Cyberneom/neom_posts.git
  neom_profile:
    git:
      url: git@github.com:Cyberneom/neom_profile.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
